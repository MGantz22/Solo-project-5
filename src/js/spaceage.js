export default class Spaceage {
    constructor(age) {
        this.age = age;
    }

    // mercuryAgeCalc() {
    //     if(this.merc !== 0) {
    //         return this.mercuryAge /= .24;
    //         } else {
    //           return this.mercuryAge;
    //         }
    //       }
};
